server:
  port: 9401

spring:
  application:
    name: auth-client

seata:
  enabled: false

logging:
  level:
    root: ERROR
    com.gf.biz: DEBUG
    org.springframework: INFO
  path: ${user.dir}/logs
  name: ${spring.application.name}
