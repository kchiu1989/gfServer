server:
  port: 9501

spring:
  application:
    name: auth-server

seata:
  enabled: false

logging:
  level:
    root: ERROR
    com.gf.biz: DEBUG
    org.springframework: INFO
  path: ${user.dir}/logs
  name: ${spring.application.name}
